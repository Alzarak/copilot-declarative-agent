{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | grep -qE '/appPackage/|/appPackage$|(manifest|declarativeAgent|apiPlugin|apiDefinition)\\.json$' && echo 'Reminder: run /copilot:validate to check the appPackage for issues.' || true",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
